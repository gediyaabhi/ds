#include<stdio.h>
int main ()
{
    int i,n,b;

    printf("enter a first value :");
    scanf("%d",&n);

    int a[n];

    for(i=0;i<n;i++)
    {
        printf("enter a first value :");
        scanf("%d",&a[i]);
    }
}